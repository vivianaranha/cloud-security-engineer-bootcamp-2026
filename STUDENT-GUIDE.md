# Student Guide

**Created by School of AI**

Think in layers: **Organization → Identity → Network → Workload → Data → Telemetry → Evidence → Response → Recovery**. Build provider-neutral controls first, then map them to AWS, Azure and Google Cloud.
